#include <snn/snn.hpp>
#include <iostream>
#include <snn/basic/perceptron.hpp>

using namespace std;
using namespace snn;

int main(int argc, char *argv[])
{
    // cout << "Multilayer Perceptron example." << endl;
    // snn::MultilayerPerceptron mlp = {10, 20, 1};

    // for (int i = 0; i < 100; ++i) {

    //     vector<double> in =  {1};
    //     vector<double> out = mlp.forward(in);
    //     // LOGD("VALUES:  ", mlp._values);
    //     LOGD("DELTAS:  ", mlp._deltas);
    //     // LOGD("WEIGHTS: ", mlp._weights);
    //     // LOGD("OUT:     ", out);

    //     vector<double> back =  {200};
    //     mlp.backward(back);

    // }
}
